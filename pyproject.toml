[project]
name = "cta-tracker"
version = "0.1.0"
description = "Kiosk-style CTA train arrival board for a wall-mounted iPad"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
]
